import logging

import iblrig  # noqa to get logging config

log = logging.getLogger("iblrig")

log.info("Blank task, used for testing...")
log.info("Blank task, used for testing...")
log.warning("...warning message...")
log.debug("...debug message...")
log.error("...error message...")
