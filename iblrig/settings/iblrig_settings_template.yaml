# For IBL, the default parameters are given in the comments below.

# The local_data_path is the path on the computer where the Subject folder containing the raw data will be saved.
# This folder can be anywhere on a writable drive, preferably not on a removable USB drive.
# If this folder does not exist, it will be created automatically upon running the first session ever.
iblrig_local_data_path: null  # C:\iblrigv8_data
# The remote_data_path is the optional location of a remote computer acting as a server to gather
# all the data in case several data streams are acquired on different PCs.
# If you are in the IBL, see TODO THESE INSTRUCTIONS on how to mount the server onto this behavior PC.
# In the IBL case, the server will look like an external drive mount point, e.g. Y:\
# If null, you will be prompted for a valid remote server path in which to save the experiment description.
# If not needed, set to False.
iblrig_remote_data_path: false  # Y:\ # for IBL, null: will prompt at session start, false: no transfer
ALYX_LAB: testlab  # required
ALYX_USER: null  # your Alyx username - can be set as null if there is no Alyx database
ALYX_URL: null  # https://alyx.internationalbrainlab.org - can be set as null if there is no Alyx database
