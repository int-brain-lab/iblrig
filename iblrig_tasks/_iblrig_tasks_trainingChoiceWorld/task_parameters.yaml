'ADAPTIVE_CONTRAST': True  # Whether to use Adaptive contrast rule or not
'AC_INIT_CONTRASTS': [1.0, 0.5]
'AC_BUFFER_SIZE': 50  # Buffer size to compute performance for each contrast
'AC_PERF_CRIT': 0.8  # Criterion for adding next contrast L AND R have to pass
'AC_NTRIALS_TO_SIX': 200  # Number of trials after 0.125 to introduce the 6% contrast
'AC_NTRIALS_TO_ZERO': 400  # Number of trials after 0.125 to introduce the 0% contrast
'AC_NTRIALS_TO_REMOVE_50': 600  # Number of trials after 0.125 to remove the 50% contrast
# ADAPTIVE_GAIN PARAMETERS (IGNORED IF ADAPTIVE_GAIN = False)
'ADAPTIVE_GAIN': True
'AG_INIT_VALUE': 8.0  # Adaptive Gain init value (azimuth_degree/mm)
'AG_MIN_VALUE': 4.0  # (azimuth_degree/mm)
'CONTRAST_SET': [1.0, 0.5, 0.25, 0.125, 0.0625, 0.0]
'CONTRAST_SET_PROBABILITY_TYPE': skew_zero  # uniform, skew_zero
'DEBIAS': True  # Whether to use debiasing rule or not by repeating error trials